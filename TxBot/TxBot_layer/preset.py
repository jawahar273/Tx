"""
The default layer are defined in this moduels
where user can extend/remove as the see fit.
"""

from TxBot_layer.preset_cmd_layers import layer_list as first_layer


# intergating diffrent layer
layer_list = first_layer
